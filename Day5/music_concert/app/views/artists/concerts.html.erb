<h1>Concerts:  </h1>

<%= flash_message(@concert) %>

<h2> <%= @artist.name %> </h2>

<ul>
  <% @concerts.each do |concert| %>
    
    <li> 
      <%= "#{concert.city} - 
           #{concert.date.strftime("%Y-%m-%d")} - 
           #{concert.price} €" %>
    </li>

  <% end %>
</ul>



<%= link_to("Back", '/artists') %>
<br>
